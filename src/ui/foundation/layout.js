import { spacing } from './tokens';

export const layout = {
  gutter: spacing.xl,
  sectionGap: spacing.lg,
  screenTop: spacing.xl,
  screenBottom: spacing.xxl,
  maxReadableWidth: 720,
};

export const withHorizontalPadding = {
  paddingHorizontal: layout.gutter,
};
